import { truncatedSummary } from '../../../helpers/truncated-summary';
import { module, test } from 'qunit';

module('Unit | Helper | truncated summary');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = truncatedSummary(42);
  assert.ok(result);
});
